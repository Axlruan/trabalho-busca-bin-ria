List<int> EncontrarTodasOcorrencias(int[] array, int valor) {
    List<int> indices = new List<int>();

    for (int i = 0; i < array.Length; i++) {
        if (array[i] == valor) indices.Add(i);
    }
    return indices;
}
